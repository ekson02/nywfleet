using Microsoft.VisualStudio.TestTools.UnitTesting;

namespace NywFleet.Tests.Controllers {
    [TestClass]
    public class TestApiControllerTest {

        [TestMethod]
        public void CreateNewTest() {
            //TestController controller = new TestController();
            //controller.Request = new HttpRequestMessage();
            //controller.Configuration = new HttpConfiguration();
            //var identity = new ClaimsIdentity("unittest");
            //identity.AddClaim(new Claim(ClaimTypes.Name, "test"));
            //identity.AddClaim(new Claim(ClaimTypes.NameIdentifier, "7878e352-6e41-46bf-af0f-7c71eb66c519"));
            //Thread.CurrentPrincipal = new GenericPrincipal(identity, null);

            //var type = new LookTestTypeCodes { TestTypeCd = "LKRDS" };
            //var response = controller.Post(type) as CreatedNegotiatedContentResult<TestPage>;
            //Assert.IsNotNull(response);
            //var pageInfo = response.Content;
            //Assert.IsNotNull(pageInfo.CurrentCategory);
            //Assert.IsNotNull(pageInfo.NextCategory);
            //Assert.IsTrue(pageInfo.Questions.Count > 0);
            //Assert.IsTrue(pageInfo.TestId > 0);
        }

    }
}
